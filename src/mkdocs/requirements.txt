# MkDocs dependencies for Guidance and Documentation
# Versions pinned to avoid accidental regressions from upstream releases.
mkdocs==1.6.1
mkdocs-shadcn==0.11.0
mkdocs-gen-files==0.6.0
mkdocs-literate-nav==0.6.2
mkdocs-redirects==1.2.2
pymdown-extensions==10.21
# Pygments 2.19+ escapes the code-block `filename` option without a
# None guard, which crashes pymdownx (it passes filename=None for untitled
# blocks). See pymdownx.highlight + pygments HtmlFormatter.
pygments==2.18.0
pyyaml==6.0.3
beautifulsoup4==4.14.3
jinja2==3.1.6
tqdm==4.67.3
